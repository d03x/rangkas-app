import Text from "@/components/Text"
import  { memo } from "react"
import { View } from "react-native"

const AccountTabScreen = ()=>{
    return <View>
        <Text>Walawe</Text>
    </View>
}

export default memo(AccountTabScreen);