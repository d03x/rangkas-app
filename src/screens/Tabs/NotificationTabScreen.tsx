import Text from "@/components/Text"
import  { memo } from "react"
import { View } from "react-native"

const NotificationTabScreen = ()=>{
    return <View>
        <Text>Walawe</Text>
    </View>
}

export default memo(NotificationTabScreen);